"""PyQt6 frontend package of the vivizoo zoo simulation.

Renders the zoo state the backend SimulationEngine reports and turns
user input into God-mode actions. It never imports ``db`` and reaches
``backend`` only through :class:`frontend.core.frontend_controller`.

Module owner: Erik (frontend).
"""

# vivizoo — Frontend (`frontend/`)
